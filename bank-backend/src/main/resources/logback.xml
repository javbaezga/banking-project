<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="stdout-classic" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>false</withJansi>
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSX, America/Guayaquil} [%thread] %highlight(%-5level) %cyan(%logger{35}) - %replace(%replace(%mdc{method} %mdc{status-code} [%mdc{request-id}]){'\[\]', ''} [%mdc{request-date}] %mdc{process-code} %mdc{caller-name} [%mdc{x-device-ip}] [%mdc{customer-affiliation-id}] [%mdc{customer-user-id}] %message){'^\s+', ''} %n</pattern>
        </encoder>
    </appender>

    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
                <timestampFormat>yyyy-MM-dd'T'HH:mm:ss.SSSX</timestampFormat>
                <timestampFormatTimezoneId>America/Guayaquil</timestampFormatTimezoneId>
                <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                    <prettyPrint>false</prettyPrint>
                </jsonFormatter>
                <appendLineSeparator>true</appendLineSeparator>
            </layout>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="${LOGBACK_APPENDER:-stdout-classic}"/>
    </root>
</configuration>